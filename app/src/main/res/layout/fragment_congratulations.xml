<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_container_FC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/job_congratulation_background"
    android:orientation="vertical">


    <TextView
        android:id="@+id/tvMessage_FC"
        style="@style/BaseTitleStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/invoice_successfull" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="right|bottom"
        android:layout_marginBottom="30dp"
        android:layout_marginRight="50dp"
        android:gravity="center_vertical"

        >

        <Button
            android:id="@+id/btnReview_FC"
            style="@style/BaseButtonStyle"
            android:layout_marginRight="30dp"
            android:text="@string/write_review"
            />

        <Button
            android:id="@+id/btnBack_FC"
            style="@style/NavigateButtonStyle"
            android:layout_width="wrap_content"
            android:text="@string/back_to_schedule"
            />

    </LinearLayout>
</FrameLayout>